
name: New Release - Publish Docker image

on:
  release:
    types: [published]
  workflow_dispatch:


jobs:
  call-publish:
    uses: ./.github/workflows/docker-publish.yml
    permissions:
      contents: read
      packages: write